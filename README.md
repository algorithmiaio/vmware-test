# VMWare Test Suite

```
docker run \
  -e VCENTER_HOST='hostname' \
  -e VCENTER_USERNAME='username' \
  -e VCENTER_PASSWORD='password' \
  -e VCENTER_TEST_DATACENTER='datacenter' \
  -e VCENTER_TEST_RESOURCE_POOL='resourcepool' \
  -e VCENTER_TEST_TEMPLATE='template' \
  <image>

```
